services:
  db:
    image: postgres:14.1
    restart: always
    environment:
      POSTGRES_PASSWORD: test
      POSTGRES_USER: test
      POSTGRES_DB: test
    ports:
      - "5432:5432"
  kafka-connect-standalone-0:
    image: kafka-connect-standalone:latest
    ports:
      - 8083:8083
    env_file:
      - .env
    environment:
      - CONNECT_BOOTSTRAP_SERVERS= #TODO insert the address of the first Kafka Cluster that you received from customer support
      - CONNECT_CONSUMER_SASL_JAAS_CONFIG=org.apache.kafka.common.security.plain.PlainLoginModule required username="$$ConnectionString" password="<use either connectionStringPrimaryKey or connectionStringSecondaryKey of the first Kafka Cluster as a password>" #TODO fill in the password
      - CONNECT_SASL_JAAS_CONFIG=org.apache.kafka.common.security.plain.PlainLoginModule required username="$$ConnectionString" password="<use either connectionStringPrimaryKey or connectionStringSecondaryKey of the first Kafka Cluster as a password>"; #TODO fill in the password
  kafka-connect-standalone-1:
    image: kafka-connect-standalone:latest
    ports:
      - 18083:8083
    env_file:
      - .env
    environment:
      - CONNECT_BOOTSTRAP_SERVERS= #TODO insert the address of the second Kafka Cluster that you received from customer support
      - CONNECT_CONSUMER_SASL_JAAS_CONFIG=org.apache.kafka.common.security.plain.PlainLoginModule required username="$$ConnectionString" password="<use either connectionStringPrimaryKey or connectionStringSecondaryKey of the second Kafka Cluster as a password>"; #TODO fill in the password
      - CONNECT_SASL_JAAS_CONFIG=org.apache.kafka.common.security.plain.PlainLoginModule required username="$$ConnectionString" password="<use either connectionStringPrimaryKey or connectionStringSecondaryKey of the second Kafka Cluster as a password>"; #TODO fill in the password
  kafka-connect-standalone-2:
    image: kafka-connect-standalone:latest
    env_file:
      - .env
    environment:
      - CONNECT_BOOTSTRAP_SERVERS= #TODO insert the address of the third Kafka Cluster that you received from customer support
      - CONNECT_CONSUMER_SASL_JAAS_CONFIG=org.apache.kafka.common.security.plain.PlainLoginModule required username="$$ConnectionString" password="<use either connectionStringPrimaryKey or connectionStringSecondaryKey of the third Kafka Cluster as a password> #TODO fill in the password";
      - CONNECT_SASL_JAAS_CONFIG=org.apache.kafka.common.security.plain.PlainLoginModule required username="$$ConnectionString" password="<use either connectionStringPrimaryKey or connectionStringSecondaryKey of the third Kafka Cluster as a password> #TODO fill in the password";
    ports:
      - 28083:8083